version: '3.8'

services:
  parking-detector:
    build:
      context: .
      dockerfile: Dockerfile
    image: parking-app:latest
    container_name: smart_parking_container
    network_mode: "host" 
    volumes:
      - .:/app 
    restart: unless-stopped